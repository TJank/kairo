# Copy this file to .env and fill in the values.
# On Windows, the path uses forward slashes relative to the prisma/ directory.

# SQLite database file — stored at prisma/dev.db relative to the project root.
DATABASE_URL="file:./dev.db"

# Optional: set your local timezone for event parsing in the ingest API.
# DASH_TIMEZONE="America/New_York"
